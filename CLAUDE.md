# CLAUDE.md

このファイルは、Claude Code (claude.ai/code) がこのリポジトリで作業する際のガイダンスを提供します。

## プロジェクト概要

**aidd.tokyo** — aidd.tokyoプロジェクトのモノレポ。以下のトップレベルディレクトリで構成される:

- `site/` — Webサイト
- `infra/` — インフラ構成
- `agents/` — AIエージェント
- `sns/` — SNS関連コード

プロジェクトは初期段階であり、各ディレクトリにはプレースホルダーのREADMEのみが存在する。
